# Papers

Upload PDFs to `raw_pdfs/`, then run `jmkms parse` from this repository (see the root `README.md`).

After parsing, each paper lives under `docs/<name>/content.md`. Add that path to `mkdocs.yml` → `nav` to include it in the site.
